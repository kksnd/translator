version: "2"
services:
  db:
    image: mysql:5.7
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./db/init/:/docker-entrypoint-initdb.d/:ro
      - ./db/my.cnf:/etc/mysql/conf.d/my.cnf
